# VirtBot Client

Клиент для VirtApp — системы управления ботами GTA5RP.

## 🚀 Быстрый старт

### Вариант 1: Готовый exe (рекомендуется)
1. Скачай последний релиз `VirtBot.exe` из [Releases](../../releases)
2. Создай файл `.env` рядом с exe:
   ```ini
   API_URL=http://gta5rp.leetpc.com/api
   ```
3. Запусти `VirtBot.exe`

### Вариант 2: Из исходников
```bash
git clone https://github.com/RootOne1337/ClientApp.git
cd ClientApp
pip install -r requirements.txt
python main.py
```

## ⚙️ Настройка (.env)

```ini
# Сервер VirtApp
API_URL=http://gta5rp.leetpc.com/api

# GTA5RP аккаунт (опционально, для синхронизации)
GTA5RP_LOGIN=your_login
GTA5RP_PASSWORD=your_password
```

## 🔄 Автообновление

Клиент автоматически проверяет обновления:
- При запуске
- Каждые 5 минут

При обнаружении новой версии — скачивает и перезапускается.

## 📝 Логи

Логи сохраняются в папке `logs/` рядом с exe.

## 🛠️ Сборка exe

Для сборки из исходников:
```bash
pip install nuitka ordered-set zstandard
build.bat
```

Готовый exe появится в `dist/VirtBot.exe`
